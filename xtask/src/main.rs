//! `xtask` — developer automation for mjx-ooxml-rs (schema codegen, etc.).
//!
//! Scaffold stub. The `codegen` command (reads `References/` XSDs and emits
//! `mjx-ooxml-types`) is implemented in Phase 0. See `PLAN.md`.

fn main() {
    println!("xtask: no command specified. Planned: `codegen` (see PLAN.md).");
}
