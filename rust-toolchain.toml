[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Cross-compile targets (wasm32-unknown-unknown, aarch64-linux-android, aarch64-apple-ios, …)
# are installed in CI rather than pinned here, so a local `cargo build` never triggers a
# network target-install. See .github/workflows/ci.yml for the cross-compile build matrix.
