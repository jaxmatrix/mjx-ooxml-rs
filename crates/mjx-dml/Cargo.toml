[package]
name = "mjx-dml"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "DrawingML: shapes, text bodies, color model, effects, preset geometry, theme (shared by all formats)."

[lints]
workspace = true

[dependencies]
mjx-ooxml-core.workspace = true
mjx-xml.workspace = true
mjx-derive.workspace = true
mjx-ooxml-types.workspace = true
mjx-mce.workspace = true
# Typed errors for the guide-formula evaluator (`geometry::formula`): a malformed or cyclic
# `a:gdLst` in an untrusted file must surface as an error, never a panic.
thiserror.workspace = true

[dev-dependencies]
# The in-context round-trip suite opens real .pptx packages to extract the parts it round-trips;
# both crates are lower layers, so these test-only edges are legal downward dependencies.
mjx-opc.workspace = true
mjx-fixtures.workspace = true
