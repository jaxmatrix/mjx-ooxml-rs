// A documentation-only module: the guide lives in `docs/effective_properties.md` so it reads as
// prose on a source host and renders as a chapter in this crate's rustdoc. `mjx-pptx` and `mjx-docx`
// carry the same module over the same file name, and `crates/mjx-ooxml/tests/
// effective_properties_shape.rs` checks the three pages still answer the same questions in the same
// order. There is no code here.
#![doc = include_str!("../docs/effective_properties.md")]
